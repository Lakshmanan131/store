<h1>Editing Student</h1>

<%= render 'form', student: @student %>

<%= link_to 'Show', edit_college_student_path(@college, @student) %> |  <!-- change -->
<%= link_to 'Back', college_students_path(@college) %> <!-- change -->
